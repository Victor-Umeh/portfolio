import { StyledWrapper } from "../components/styled/elements.styled";

const Styled = () => {
  return (
    <StyledWrapper pad="2rem" border="1px solid grey">
      styled
    </StyledWrapper>
  );
};

export default Styled;
